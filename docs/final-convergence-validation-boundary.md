# Final convergence validation boundary

Source merge is not deployment proof. The exact branch head requires isolated preview evidence and manual approval. After an approved merge, production is accepted only after the automatic deployment workflow verifies the exact merge commit on `atlas-systems.uk` and completes its browser smoke checks.
