/**
 * Deprecated compatibility stub.
 *
 * Homepage estate-strip state and DOM ownership moved to js/live-signal.js.
 * atlas-registry.js remains the shared acquisition client.
 *
 * This file is intentionally side-effect free while the existing classic-script
 * include remains in index.html. The include will be removed during the native
 * ES module migration so ownership and module wiring change in separate steps.
 */
